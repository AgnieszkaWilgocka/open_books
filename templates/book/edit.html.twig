{% extends 'app.html.twig' %}

{% block body %}
	<div class="form-container">
		{% include "book/_form.html.twig" with {action_title: 'Update'} %}
	</div>
	{# {{ form_start(form, {'action': path('book_edit', {'id': book.id}), 'method': 'PUT'}) }}
	{{ form_row(form.fileName) }}
	{{ form_rest(form) }}
	{{ form_end(form) }} #}
{% endblock %}
