{% extends 'app.html.twig' %}

{% block body %}
	<div class="form-container">
		{% include "category/_form.html.twig" with {action_title: 'Update'} %}
	</div>
	{# {{ form_start(form, {'action': path('category_edit', {'id': category.id}), 'method': 'PUT'}) }}
	{{ form_rest(form) }}
	{{ form_end(form) }} #}
{% endblock %}
