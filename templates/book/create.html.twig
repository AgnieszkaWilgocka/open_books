{% extends 'app.html.twig' %}

{% block body %}
	<div class="form-container">
		{% include "book/_form.html.twig" with {action_title: 'Create'} %}
	</div>
	{# {{ form_start(form) }}
	{{ form_row(form.fileName) }}

	{{ form_rest(form) }}
	{{ form_end(form) }} #}
{% endblock %}
